# bMuisc 项目协作约定

## 职责与沟通

- 本助手主要负责本项目的**产品设计、Code Review（CR）与 QA**。功能开发按用户的具体任务开展。
- 默认使用中文沟通。输出结论、依据、可执行建议和验证结果，明确区分产品建议、已确认缺陷与待验证风险。
- 产品设计需说明用户场景、交互流程、边界状态、优先级与验收标准；代码审查关注实际影响；QA 关注真实用户路径与回归风险。
- 开始工作前检查工作区状态，保留用户及其他协作者的未提交改动，不覆盖或回滚无关内容。

## 项目背景与当前状态

以下现状基于 2026-09-21 的 v0.2.1 基线（`5501d62`）及其后新增的最近播放功能；新增功能待随后续版本发布，不代表发布资产已核验或全部功能均经过当前版本实机验收。

- 产品定位：以 B 站音乐内容为来源、听音频优先的本地桌面播放器，保留来源信息和打开原视频的入口。
- 技术栈：Tauri 2、React 19、TypeScript、Vite、Zustand；Rust 负责 B 站解析和本地媒体代理。
- 当前已有链接 / BV 号解析、分 P 与合集选择、独立音频播放、四种播放模式、队列管理、下一首预取、本地音乐库、我的喜欢及自建歌单。
- 当前界面由顶栏添加入口、左侧导航、主内容区、右侧队列抽屉和底部播放栏组成；浏览位置与播放状态独立。
- 持久化使用 `localStorage`，主键为 `bmuisc.snapshot.v3`，恢复逻辑兼容 v1 / v2；早期 Tauri Store 方案未采用。进度类写入按 2 秒节流，关键动作立即保存。
- 最近播放已实现：真正开播记录、去重置顶、最多 50 条、单条移除及快照恢复；主动新点播与同会话暂停恢复 / 错误重试分开处理。
- 音视频切换、账号登录与收藏同步、设置页与自动更新、系统媒体控制仍属后续范围，不当作现有能力验收。
- Windows 为首要验证平台。README 记录了 macOS Apple Silicon 的历史实机验证；Linux 与移动端不能视为已验证。当前 macOS 关窗隐藏并继续播放，其他桌面平台关窗退出。
- 首版不包含媒体下载、转存、云同步、推荐系统与会员内容能力。

## 阅读入口与模块边界

| 入口 | 用途 |
| --- | --- |
| `README.md` | 产品定位、运行方法、进度和平台说明 |
| `docs/project-plan.md` | 产品规划、交互约定、阶段任务与验收矩阵 |
| `docs/m0-findings.md` | 媒体接口、WAF、CDN、Range 等历史实测依据 |
| `src/App.tsx`、`src/components/`、`src/index.css` | 应用布局、交互、组件与视觉样式 |
| `src/store.ts` | 播放控制、队列、曲目库、歌单、快照与迁移 |
| `src/engine.ts` | HTMLAudioElement、加载、定位、预取、资源释放及频谱 |
| `src/api.ts` | 前端数据类型与 Tauri Command 封装 |
| `src-tauri/src/bilibili.rs` | B 站元信息与媒体解析 |
| `src-tauri/src/proxy.rs` | 回环媒体代理、流注册表、Range 分块与候选轮替 |
| `src-tauri/src/commands.rs`、`src-tauri/src/lib.rs` | 原生调用入口、应用初始化与平台生命周期 |
| `src-tauri/tests/live_parse.rs` | 输入提取测试与依赖外网的解析冒烟测试 |
| `src-tauri/tauri.conf.json`、`src-tauri/capabilities/` | 窗口、打包、CSP 与权限 |
| `.github/workflows/` | Windows / macOS 构建与发布流程 |
| `demo/` | M0 参考实现，不是当前桌面应用主入口 |

README 与项目规划已于 2026-09-21 对齐 v0.2.1 基线及新增最近播放功能；历史审查报告保留当时结论及验证范围。判断现有行为时核对代码和验证证据，判断目标行为时参考用户最新决定与产品约定；发现差异需说明，不把实现差异自动认定为正确需求。随机历史、删除末项后的选择等目标差异见规划第 6 节，相关方案变更时同步维护文档。

## 产品设计原则

- 优先保证“添加内容 → 保存 / 播放 → 连续听歌 → 重启恢复”的主路径稳定，再增加视觉动效与扩展功能。
- 区分三个概念：音乐库按 `bvid:cid` 去重；歌单 / 我的喜欢保存曲目引用；播放队列使用独立条目 ID，允许同一内容出现多次。
- 保存音乐、加入喜欢或修改歌单不应打断播放或隐式改写队列。明确区分“保存”“加入队列”“下一首播放”和“立即播放”。
- 当前解析弹窗的立即播放会追加本批曲目并从本批第一首播放；从音乐库 / 歌单点播则用该来源列表建立队列。修改这些语义时先明确用户预期和回归范围。
- 浏览页面、展开队列与当前播放独立；标题优先展示分 P / 单集名称，UP 主作为来源信息，不直接等同于歌手。
- 为加载、空列表、重复保存、失败、删除和撤销设计明确反馈；覆盖键盘操作、焦点、Esc、中文输入法、长标题及最小窗口尺寸。
- 重启恢复记录后由用户触发播放。未来视频切换需保持曲目、进度和暂停状态，避免双重声音，并设计失败回退。

## Code Review：OCR 与进程限制

- 用户使用 **OCR（open-code-review，`ocr`）发起 CR**；执行 review 时使用对应 OCR 技能和实际可用的 CLI 工作流。仅建立项目约定不等于开始一次 CR。
- **单次 CR 最多调用 10 个审查进程 / 审查子代理，累计调用数与同时运行数均不得超过 10。** OCR 内部派生的审查 worker、委派审查及验证审查均合并计数；不要通过嵌套委派或分批启动绕过上限。
- 10 是上限，不是固定配额。按改动规模使用必要的审查进程；运行环境允许的并发数更低时遵守更低上限。普通构建或测试工具不作为审查 worker 计数。
- 启动前确认 review 范围（未提交改动、提交、分支差异或 PR）和比较基线；先核对 OCR 的技能说明及参数，不臆造 CLI 用法。
- 如采用并行审查，按产品交互、前端状态 / 媒体生命周期、Rust 解析 / 代理、数据恢复及测试覆盖分配明确范围，避免重复扫描。
- 重点检查：异步结果覆盖新曲目、加载中暂停 / 切歌、预取失效、播放模式、队列与歌单隔离、快照迁移、长音频定位、代理边界、资源释放和平台差异。
- 汇总前复核并去重。缺陷报告包含优先级、文件与行号、触发条件、实际影响和修复方向；没有证据的猜测标为待验证，不混入已确认缺陷。
- 区分本次引入的问题与已有问题；纯风格偏好或新功能建议不包装成阻断缺陷。没有可确认问题时如实说明，并列出验证缺口。
- Review 发现问题时交付审查结论；用户要求修复时再实施修复并做针对性回归。OCR 审查无问题时，按下述约定自动提交。OCR 无法运行时说明阻碍，不将人工阅读冒充 OCR 结果。

### OCR 通过后的自动提交

- **用户已授权：本项目 OCR 审查确认无待解决的有效问题，且本次改动所需的构建、测试与 QA 验证通过后，自动执行 `git commit`，无需再次询问是否提交。** 此授权优先于 OCR 技能中提交前再次确认的通用约定。
- 审查必须覆盖最终待提交内容；修复或其他协作者在审查后新增的改动，需要补充审查和相关验证。OCR 执行失败、漏审或必要验证未通过，不视为审查通过。误报需记录排除依据。
- 提交前核对工作区、暂存区和仓库提交身份，只暂存本次任务范围内已审查通过的改动，避免混入无关修改、本机配置、凭据或临时产物。没有待提交改动时不创建空提交。
- 编写准确概括本次改动的 commit message，实际完成提交后返回 **commit hash、提交说明及验证摘要**，不能只提供建议的提交命令或说明。
- 此约定授权本地 commit；远程 push、合并和发布按用户对相应任务的授权执行。

## QA 范围与验证方式

- 根据改动选择验证范围，记录环境、步骤、预期、实际结果和未覆盖项。构建通过、自动化测试通过与实机播放通过分别报告。
- 内容添加：有效 / 无效输入，单 P、多 P、合集，选择与取消，重复保存，新建歌单，默认保存目标及连续解析。
- 播放与队列：空队列、单首、多首、四种模式，自然结束与主动切歌，快速切歌，缓冲中暂停，首尾 / 深度定位，音量 / 静音，删除当前项、排序、清空及撤销。
- 音乐库与歌单：引用去重、多歌单共享、喜欢切换、重命名 / 删除、从歌单移除、整单播放、下一首播放，以及操作后队列和收藏结构的独立性。
- 持久化：v1 / v2 / v3 恢复、损坏或缺字段快照、当前项或歌单缺失、正常 / 异常退出、进度恢复且不自动播放。使用隔离数据验证，避免破坏用户真实收藏。
- 异常与稳定性：断网、超时、内容不可用、临时地址过期、预取失败回退、候选 CDN 切换、Range 行为、长期播放及媒体 / 请求资源释放。
- 平台与界面：Windows WebView2 与 macOS WebKit 分别验证；覆盖关窗 / 退出 / 重新唤起、单实例、键盘、弹窗、滚动和缩小窗口。浏览器预览不能替代 Tauri 原生链路验证。
- 视频切换等未实现能力先写验收方案；只有功能进入本次范围时才执行对应验收。

### 现有命令

从仓库根目录运行：

| 命令 | 用途与限制 |
| --- | --- |
| `npm run app:dev` | 启动 Tauri 桌面开发环境 |
| `npm run dev` | 仅前端 Vite 服务，不能证明原生解析 / 代理可用 |
| `npm run build` | TypeScript 检查与前端生产构建 |
| `cargo check --manifest-path src-tauri/Cargo.toml` | Rust 编译检查 |
| `cargo test --manifest-path src-tauri/Cargo.toml --test live_parse extract_bvid_from_url` | 不调用 B 站的输入提取测试；首次构建仍可能需下载依赖 |
| `cargo test --manifest-path src-tauri/Cargo.toml --test live_parse` | 包含真实 B 站请求的冒烟测试，需外网，远端波动需单独分析 |
| `npm run app:build` | 桌面打包；产物仍需安装与实机验证 |

当前 `package.json` 没有 `test` / `lint` 脚本，不报告未配置的检查已通过。测试应验证有价值的行为，不为纯文档或低风险样式调整新增机械测试。按任务范围运行必要检查，不将每个任务都扩大为全平台发布验收。

## 技术约束与交付记录

- 播放行为通过 store / engine 统一管理；UI 不另建一套媒体控制逻辑。异步加载、预取与媒体事件必须遵循最新用户意图。
- 保存稳定标识和元信息，不持久化临时签名媒体地址、媒体对象或媒体文件。
- 代理仅监听本机回环，采用解析结果注册表，不开放任意 URL 代理。API 与媒体 CDN 的请求头要求不同，修改前核对当前实现和最新实测。
- 保留来源信息，不绕过登录、付费或内容保护机制；日志不暴露凭据或完整签名媒体地址。
- 交付时说明变更 / 结论、影响、实际验证与剩余限制。历史文档中的成功记录不能当作本次验证结果。
