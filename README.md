# bMuisc

基于 B站视频内容的跨平台音乐播放器：以听音频为主，支持打开 B 站原页面。应用内音视频切换仍在规划中。

> **当前状态（2026-09-21）**：当前代码版本为 **v0.2.1**，最近播放功能在该版本基线上新增，待随后续版本发布。已实现音频播放器、音乐库、我的喜欢、自建歌单、最近播放和快照 v3。既有 Windows / macOS 音频流程有历史实机记录；自动化验证不等同于当前版本全平台实机验收。详见[当前进度](#当前进度)。

## 产品定位

让 B 站的音乐现场、演唱会、翻唱和音乐合集拥有方便的听歌体验。当前支持连续听音频；在同一进度切换视频画面是后续产品目标。以下音视频相关原则描述目标能力。

核心原则：

- **听歌优先**：默认展示封面、标题、UP 主和播放控制，减少画面对注意力的占用。
- **随时观看**：展开或收起视频时，保持当前曲目、进度和播放状态。
- **队列统一**：音频和视频模式共用播放队列、循环模式和收藏记录。
- **来源清楚**：保留视频来源、作者信息和跳转到 B 站原页面的入口。

## 平台范围

使用 **Tauri 2** 构建跨平台应用，共享前端界面和可复用的 Rust 逻辑。

| 平台 | 计划 |
| --- | --- |
| Windows | 首个开发与验证平台 |
| macOS | 已实机验证：Apple Silicon（M4 / macOS 27）跑通音频播放全流程（2026-09-18） |
| Linux | 待适配验证 |
| Android / iOS | 后续评估，复用界面和业务逻辑，按需实现原生能力 |

Tauri 支持多个平台，但 WebView 的媒体格式支持、后台播放、系统媒体控制和权限机制存在差异。移动端不作为第一版交付目标，各平台需要分别构建和测试。

## 第一版范围

当前音频版路径：**粘贴视频链接或 BV 号 → 选择分 P / 合集条目 → 保存到音乐库 / 喜欢 / 歌单 → 按需立即播放或从列表点播**。音视频切换已移至阶段四。

### 内容添加

- 接受常见 B 站视频链接和 BV 号，校验输入并读取可用的视频元信息。
- 展示标题、封面、UP 主、时长及分 P 列表。
- 多分 P 与合集允许选择条目，保存目标支持音乐库、我的喜欢、已有或新建歌单；单纯保存不改播放队列。
- 勾选“添加后立即播放”会将本批曲目追加到队列并从本批第一首播放；从音乐库或歌单点播会用该来源列表建立队列。
- 内容不可用、需要登录或访问受限时，提供明确提示和打开原页面的入口。

### 播放控制

- 播放 / 暂停、上一首 / 下一首、拖动进度、音量调节。
- 顺序播放、单曲循环、列表循环、随机播放。
- 队列添加、移除、清空及调整顺序。
- 音视频切换及其进度、音量和播放状态衔接：阶段四待实现。
- 播放出错时允许重试或跳过，避免无限重试。

### 本地记录

- 保存音乐库、喜欢、歌单、最近播放、播放队列、进度、音量和循环模式。
- 重启后恢复队列和上次进度，由用户触发继续播放。
- 使用稳定的视频标识保存记录，不把临时媒体地址作为持久数据。

### 后续功能

- 音视频切换、设置页与自动更新。
- 内容搜索、收藏夹导入及必要的账号能力，先确认接口和授权方式的可行性。
- 系统媒体控制、媒体快捷键、托盘与桌面端后台播放。
- 移动端后台播放、锁屏控制和音频焦点管理。
- 歌词与章节信息，前提是有可用且允许使用的数据来源。

首版暂不包含离线下载、媒体转存、云端同步、推荐系统和会员内容能力。

## 界面与后续规划

- **当前布局**：顶栏添加入口、左侧音乐库 / 喜欢 / 歌单导航、主区列表或正在播放页、右侧可收起队列、底部播放栏；浏览页面不打断播放。
- **底部播放栏**：封面、标题、播放控制、进度条、音量、循环模式及队列开关；点击封面进入正在播放页。
- **最近播放**：侧栏独立入口，支持点播、搜索和单条移除。
- **后续页面**：视频区域及全屏、视频开关、设置。
- **状态反馈**：清楚区分正在解析、正在缓冲、需要登录、内容失效和播放失败。

## 技术方案

| 层级 | 技术选择 | 职责 |
| --- | --- | --- |
| 应用容器 | Tauri 2 | 窗口、权限、平台集成和应用打包 |
| 前端 | React + TypeScript + Vite | 界面、交互和页面组织 |
| 状态管理 | Zustand | 播放状态、队列与界面状态 |
| 原生逻辑 | Rust + Tauri Commands | 内容源访问、媒体地址解析和受控的本地能力 |
| 播放层 | HTMLMediaElement，必要时接入 MSE 播放方案 | 媒体加载、播放、定位和音视频同步 |
| 本地存储 | WebView localStorage，快照 v3 | 保存音乐库、喜欢、歌单、队列与续播状态，兼容 v1 / v2 恢复 |

如果后续歌单和历史记录规模增长，再评估 SQLite。首版以本地应用为主，不部署自有媒体服务。

### 模块边界

1. **内容源适配器**：输入视频标识，返回元信息、分 P 和当前可用的媒体信息；隔离平台接口变化。
2. **播放控制器**：统一管理播放状态、进度、队列、模式切换和失败恢复。
3. **媒体引擎**：负责具体媒体加载与同步，通过统一接口向播放控制器报告事件。
4. **本地存储**：保存稳定标识、元信息快照、用户设置及播放进度。
5. **平台适配层**：封装媒体快捷键、系统媒体会话和后台运行等平台差异。

前端通过受限的 Tauri Commands 调用原生能力。网络请求仅面向允许的内容源与必要的媒体域名，不提供可任意访问 URL 的通用代理。

### 媒体解析与播放

1. 解析用户输入，取得视频及分 P 的稳定标识。
2. 读取元信息，检查当前访问条件及可用媒体格式。
3. 优先验证独立音频流能否在目标平台稳定播放；音频模式只加载音频时，才有节省视频流量的效果。
4. 后续视频能力：打开视频时取得对应资源，在当前音频时间点定位并同步。
5. 后续视频能力：收起时释放视频资源，同时保持音频连续播放。
6. 临时媒体地址失效时，根据稳定标识重新解析，并尽可能恢复当前进度；重试次数有限。

不能假定一个视频地址可直接交给 `<video>` 播放。若平台提供分离的音视频轨，需要选择支持该形式的播放方案，并处理缓冲、拖动进度和同步。具体使用原生媒体能力还是 MSE，应由第一阶段验证结果决定。

当前已采用仅监听本机回环的注册表媒体代理，转发解析登记的音频地址并处理 Range 与候选节点回退；不注册、不传输视频轨。后续视频方案仍需单独验证。

### 音视频切换（阶段四待实现）

- 以统一播放状态为准，记录当前曲目、目标进度、暂停状态和音量。
- 展开视频时，在资源准备完成后定位到当前播放点，避免重复播放两路音频。
- 暂停状态下切换模式，仍保持暂停；视频加载失败时，尽可能继续原有音频播放。
- 对加载、切歌和切换模式使用可取消的操作，防止旧请求覆盖新曲目。
- 验证连续切换、拖动进度、缓冲和切歌时的同步表现，不预先承诺无缝切换。

### 数据与隐私

- 队列项包含视频标识、分 P 标识、标题、作者、封面和时长等信息。
- 临时媒体地址按需获取，不写入长期记录或日志。
- 首版不要求账号登录；如后续需要登录，应由用户主动授权，敏感凭据交给系统安全存储，避免明文写入普通配置。
- 对平台请求设置超时、有限重试与并发限制，不进行批量抓取。

## 实施阶段与验收

### 阶段一：验证播放可行性 ✅ 完成

在 Windows 上选取普通视频和多分 P 内容，验证元信息、媒体地址、独立音频及进度定位。视频播放与切换验证已移至阶段四。

验收重点：独立音频是否可用、实际传输资源及 WebView 编解码兼容性；已据此确定 HTMLAudioElement 与本地代理方案。

实测结论：独立音频流匿名可用（DASH 三档 AAC），WebView2 播放 fMP4/AAC 正常；WAF 按请求形态拦截（详见 [M0 技术验证记录](docs/m0-findings.md) §2.1）；签名媒体地址有效期很短，"每次播放前重新解析"成为既定架构。音视频切换尚未验证。

### 阶段二：完成最小播放器 ✅ 完成

实现链接添加、分 P / 合集选择、播放队列、基础控制、循环模式和本地恢复。

验收重点：从添加内容到完整音频播放的路径可用；切歌、暂停、定位和重启恢复行为正确。

音频路径已实机跑通（含多分 P 与合集），本地持久化（队列 / 进度 / 音量 / 模式）已就绪；音视频切换按 2026-09-18 调整移至阶段四，与账号登录同期实现。

### 阶段三：完善持久化与异常处理 🔶 进行中

增加设置和播放记录保存、媒体地址刷新、网络错误处理和内容失效提示。

验收重点：重启可恢复记录；断网或地址过期不会使应用卡死；失效内容可以重试、移除或跳过。

快照 v3、坏档备份与旧版恢复、失败音源重试、会话隔离、暂停意图、预取回退、代理异常处理及最近播放已实现并完成针对性复审；设置页随自动更新安排到阶段四，当前版本的完整实机异常矩阵仍未完成。

### 阶段四：扩展平台与体验 🔶 部分提前完成

本地音乐库、喜欢和歌单已提前完成；macOS 基础音频流程已有历史实机记录。音视频切换、账号登录与收藏同步、系统媒体控制、Linux 与移动端适配仍未实现；视频访问范围需按登录状态和内容权限验证。

验收重点：按平台检查格式支持、后台行为、资源占用和打包结果，不以单个平台的成功替代跨平台验证。

## 当前进度

截至 2026-09-21，版本基线为 **v0.2.1**（`5501d62`，package.json / Cargo.toml / Tauri 配置一致），最近播放为此基线后的新增实现，待随后续版本发布。此处描述仓库实现进度；远程 Release 与安装包状态以 [Releases](https://github.com/roy2651/bMuisc/releases) 为准，本次未重新核验发布资产。

桌面端已实现：

- **内容添加**：粘贴链接或 BV 号 → 解析元信息（标题 / UP 主 / 封面 / 时长）；多分 P 与合集（ugc_season）双 Tab 选择，支持全选 / 清空，合集条目带各自封面、时长、UP 主。
- **音频播放**：DASH 独立音轨，按码率自动选档；经本地代理拉流（注册表模式：只转发解析登记过的地址；官方 CDN 镜像优先、失败自动轮替；开放式 Range 切成有界分块）。61 分钟长视频实网压测通过：深定位（20%~99% 往返）、候选轮替、连续拉流至片尾均无错误（2026-09-20）。
- **播放控制**：播放 / 暂停、上一首 / 下一首、拖动进度、音量（含百分比显示）、顺序 / 单曲循环 / 列表循环 / 随机四种模式。
- **队列管理**：当前项标记（跳动条动画）、点击播放、移除、清空、上移 / 下移。
- **持久化**：`localStorage` 快照 `bmuisc.snapshot.v3` 保存库、喜欢、歌单、队列与续播状态，兼容 v1 / v2；坏档备份后尝试恢复旧版或重建。进度类每 2 秒节流，关键动作立即保存；重启后由用户触发续播。
- **界面**：深色主题（B 站粉 + 紫渐变强调色），BM 应用图标；顶栏品牌区 + 正在播放大卡片改版，播放频谱波浪（真实 FFT，失败自动降级合成波浪）。
- **测试**：Rust 解析链路实网冒烟测试（`cargo test --test live_parse`，依赖外网）。
- **macOS 适配**：Apple Silicon（M4 / macOS 27）实机验证通过——解析、本地代理拉流、播放、持久化恢复均正常（2026-09-18）。关窗即隐藏窗口（后台继续播放），Dock 点击唤回，Dock 右键 / Cmd+Q 才退出。v0.1.5 补齐 WebKit 细节：SF Pro / 苹方字体栈、禁滚动橡皮筋、文字渲染调优、封面禁原生拖拽、解析弹窗 Esc 关闭。
- **秒切下一首**：开播后按循环模式预解析并预缓冲下一首，命中预取时切歌毫秒级出声（单曲循环同曲重播零解析直接定位）；预取超时或未命中自动回退常规解析，加载状态有明确提示。
- **曲目库与歌单**：全部音乐、我的喜欢、自建歌单的创建 / 重命名 / 删除与曲目管理；解析弹窗可选音乐库 / 喜欢 / 已有或新建歌单，并可勾选立即播放。保存与播放分离；快照只保存元信息、引用及状态，不落媒体文件。
- **稳定性修复**：进度保存、坏档恢复、旧媒体事件隔离、失败后的主播放按钮重试、解析期间暂停、预取失败回退与资源释放、代理 5xx 候选切换和后缀 Range、元信息刷新、窄窗导航及键盘操作入口。

**验证进度**：`3bdbc0d` 对应复审完成 18 项正向隔离探针、前端构建和 Rust 离线输入测试，详见 [复审报告](docs/reviews/2026-09-21-r123-verified.md)。随后 `853d1ed` 补充终败时暂停旧源、无源解析窗口连续点击时保留续播点；本次仅核对实现与文档，未把前轮测试结果当作该提交的新验收。v0.2.0 的桌面实机播放、窄窗 / 键盘与跨平台完整验收仍需补齐。

- **最近播放**：真正开始播放才记录，按最后开播去重置顶，最多 50 条；移除不影响音乐库、喜欢、歌单或队列。同会话暂停恢复 / 错误重试不撤销移除，新的主动点播可重新记入。快照 v3 增量保存，旧档恢复为空列表。14 项正向隔离回归及前端构建通过，另完成 Edge 同源重新定位的媒体事件验证，见 [最近播放复审验收](docs/reviews/2026-09-21-recent-verified.md)。

尚未实现：音视频切换（计划与账号登录一起实现）、设置页与自动更新、账号登录与收藏同步、系统媒体控制、Linux 适配、macOS 打包公证。

## 运行

```bash
npm install
npm run app:dev   # 开发运行（桌面窗口 + 前端热更新）
```

要求 Node 18+、Rust 1.88+（依赖链要求，`rustup default stable` 即可）、Windows 10+（WebView2）或 macOS。打包：`npm run app:build`（Windows 出 NSIS 安装包，macOS 出 .app/.dmg）。

## 下载安装

到 [Releases](https://github.com/roy2651/bMuisc/releases) 下载对应平台的安装包：

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| Windows 10+ | `*-x64-setup.exe` | 直接运行安装 |
| macOS（Apple Silicon） | `*-aarch64.dmg` | M 系列芯片（M4 实测） |
| macOS（Intel） | `*-x64.dmg` | Intel 芯片 |

**macOS 首次打开说明**：安装包未做 Apple 公证（无开发者账号），首次打开会被系统拦截。按以下任一方式放行：

1. **系统设置 → 隐私与安全性**：先尝试打开应用一次，被拦截后进入该设置页，滚到底部会提示「已阻止…因为来自身份不明的开发者」，点击**「仍要打开」**即可（推荐，各版本系统通用）；
2. 终端执行：`xattr -cr /Applications/bMuisc.app`（清除隔离标记，若提示「应用已损坏」用这条解决）；
3. 较旧系统（macOS 13/14）也可在访达中**右键点击应用 → 打开 → 再点「打开」**。

## 主要约束

- B 站接口、访问要求和媒体地址策略可能变化，内容源适配器需要持续维护。
- 免费观看不代表允许下载、转存或再分发；应用仅处理用户有权访问、且平台规则允许使用的内容，不绕过登录、付费或内容保护机制。
- 本项目为独立项目，与 B站无官方关联；内容权利归原作者及相关权利人所有。
- 在确认媒体访问方式和使用条件可行后，再投入完整工程开发。

## 当前仓库

```
src/               前端（React + Zustand）：界面组件、播放引擎封装、状态管理
src-tauri/         桌面端（Rust）：B 站解析适配器、本地媒体代理、Tauri Commands
src-tauri/tests/   解析链路实网冒烟测试
demo/              M0 时期的网页演示（零依赖 Node 服务），保留作参考实现
docs/              项目规划与 M0 技术验证记录
scripts/           应用图标生成脚本（PowerShell + GDI+）
```

- 开发顺序、架构边界、交互规则和验收标准见 [项目规划](docs/project-plan.md)。
- 媒体访问的实测数据（WAF 拦截矩阵、CDN 特性、地址有效期）与由此产生的架构调整见 [M0 技术验证记录](docs/m0-findings.md)。
